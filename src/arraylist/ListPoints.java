package arraylist;

public class ListPoints<I extends Number> {
    //It is a data structure where we can store elements in a contiguous fashion
    //contiguous means touching or adjacent to (one element is adjacent to another)
    //continuous means uninterrupted or without break
    // List in Java provides the facility to maintain the ordered collection.
    // It contains the index-based methods to insert, update, delete and search the elements.
    // It can have the duplicate elements also. We can also store the null elements in the list.

    //Methods of list
    // List<Integer> list = new ArrayList<>();
    // For adding element [O(n)]- list.add(1); or list.add(index:1, element:56);
    // List<Integer> newList = new ArrayList(); - newList.add(150);
    // Adding one whole list into another list
    // list.addAll(newList);
    // To get a particular element - list.get(index:5)
    // To remove element - list.remove(index:2) or list.remove(Integer.valourOf(150))
    // To remove all list - list.clear()
    // To update element [O(1)]- list.set(index:2 , value:300);
    // To check particular element is present or not - list.contains(element:50)
}
