package queueandlinkedlist;

public class QueuePoints {

    //First In First Out Data Structure (FIFO)
    // Two ends front end and rear end
    // add from rear
    // delete from front

    //Methods of queue
    //Queue<Integer> queue = new LinkedList<>();
    // To add element - queue.offer(1);
    // To delete element (first element would be deleted ) - queue.poll()
    // To see element (first element to get added ) - queue.peek();
}
